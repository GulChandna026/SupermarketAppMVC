<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/styles.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Order History - Supermarket App</title>
  <style>
    .order-card {
      background: white;
      border: 2px solid #e8e8e8;
      border-radius: 10px;
      padding: 20px;
      transition: all 0.3s ease;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }

    .order-card:hover {
      box-shadow: 0 8px 20px rgba(102, 126, 234, 0.3);
      border-color: #667eea;
      transform: translateY(-5px);
    }

    .order-card-header {
      border-bottom: 2px solid #f0f0f0;
      margin-bottom: 15px;
      padding-bottom: 15px;
    }

    .order-id {
      font-size: 1.3rem;
      font-weight: bold;
      color: #2c3e50;
    }

    .order-date {
      font-size: 0.85rem;
      color: #7f8c8d;
      margin-top: 5px;
    }

    .order-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10px;
      padding: 10px 0;
    }

    .order-label {
      color: #7f8c8d;
      font-weight: 600;
    }

    .order-value {
      color: #2c3e50;
      font-weight: bold;
    }

    .order-total {
      font-size: 1.4rem;
      color: #27ae60;
      margin: 15px 0;
      text-align: center;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">🛒 Supermarket App</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/shopping">🛍️ Shop</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart">🛒 Cart</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/order-history">📦 Order History</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/logout">🚪 Logout</a>
          </li> 
        </ul>
      </div>
    </div>
  </nav>

  <!-- Container -->
  <div class="container">
    <div class="welcome-text mb-3">Welcome, <strong><%= user.username %></strong>!</div>
    <h2 class="page-title">📦 Order History</h2>

    <% if (orders && orders.length > 0) { %>
      <div class="mt-4">
        <% orders.forEach(order => { %>
          <div class="order-card">
            <div class="order-card-header">
              <div class="order-id">Order #<%= order.id %></div>
              <div class="order-date"><%= new Date(order.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></div>
            </div>

            <div class="order-info">
              <span class="order-label">Items:</span>
              <span class="order-value"><%= order.item_count %></span>
            </div>

            <div class="order-total">
              $<%= parseFloat(order.total).toFixed(2) %>
            </div>

            <a href="/order-details/<%= order.id %>" class="btn btn-primary w-100">View Details</a>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="alert alert-info mt-4">
        <p class="mb-0">You have no orders yet.</p>
        <p class="mb-0"><a href="/shopping" class="alert-link">Start shopping →</a></p>
      </div>
    <% } %>
  </div>
</body>
</html>
